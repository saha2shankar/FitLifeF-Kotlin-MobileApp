package np.com.harishankarsah.fitlife.ui.theme

import androidx.compose.ui.graphics.Color

// Primary & Secondary Brand Colors
val Primary = Color(0xFF00875F)        // Main brand green
val PrimaryVariant = Color(0xFF00664A) // Darker shade for buttons, AppBar
val OnPrimary = Color(0xFFFFFFFF)      // Text/icons on primary

val Secondary = Color(0xFFF34E3A)      // Accent / secondary brand red
val OnSecondary = Color(0xFFFFFFFF)    // Text/icons on secondary

// Background & Surface
val Background = Color(0xFF141516)     // Dark background for app
val OnBackground = Color(0xFFFFFFFF)   // Text on background
val Surface = Color(0xFF1E1E1E)        // Cards, sheets, dialogs
val OnSurface = Color(0xFFFFFFFF)      // Text on surface

// Accent / Highlight
val Accent = Color(0xFFE6FE58)         // Optional highlight / glow
val OnAccent = Color(0xFF000000)       // Text on accent

// Semantic Colors
val Error = Color(0xFFFF5252)          // Error messages
val OnError = Color(0xFFFFFFFF)

val Success = Color(0xFF4CAF50)        // Success / completed workout
val OnSuccess = Color(0xFFFFFFFF)

val Warning = Color(0xFFFFC107)        // Warning messages
val OnWarning = Color(0xFF000000)

val Info = Color(0xFF2196F3)           // Info / notifications
val OnInfo = Color(0xFFFFFFFF)
