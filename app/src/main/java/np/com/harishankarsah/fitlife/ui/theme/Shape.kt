package np.com.harishankarsah.fitlife.ui.theme

import android.util.Size
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

// FitLife Shapes for Material3
val Shapes = Shapes(
    small = RoundedCornerShape(6.dp),  // Used for buttons, chips, small components
    medium = RoundedCornerShape(6.dp), // Cards, dialogs, sheets
    large = RoundedCornerShape(6.dp)   // Large containers or bottom sheets
)
